services:
  mysql:
    image: mysql:8.4
    container_name: aerowatch-db-mysql
    restart: always
    ports:
      - "3306:3306"
    env_file:
      - .env
    volumes:
      - ./mysql:/var/lib/mysql
    networks:
      - aerowatch

networks:
  aerowatch:
    external: true